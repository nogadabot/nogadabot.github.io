# 代理选择标准

代理不是必买项目。直接合约铸造通常不使用代理，路径更短。只有多个钱包同时调用签名/voucher API，或用户明确要求特定出口路径时才需要考虑。

## 选择前检查

* 能否连接目标链 RPC 与所需 API 域名
* 在应用**全部测试**中延迟和失败率是否稳定
* 地区、并发连接数、流量上限与认证方式
* 故障行为是否符合任务的 fail-closed 路径策略；Nogada 不会暗中改为直连

格式：

```text
host:port:username:password
```

服务商、价格与条款经常变化。购买前请检查当前口碑与退款条件，并先用小额测试。
