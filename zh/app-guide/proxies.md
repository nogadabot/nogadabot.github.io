# 代理
代理会让请求经过你指定的网络路径。并非所有铸造都需要代理，增加网络路径也可能带来额外延迟。

在**高级菜单 → 代理**中创建分组，按支持的格式导入，例如：

```text
host:port:username:password
```

导入后可使用分组测试和主机筛选。删除项目或清空分组前，请核对界面显示的范围。

如果任务指定了代理组，请确认所需 RPC 和铸造数据服务能通过该路径访问。指定路径失败时，不应悄悄改用直连并显示成功。

不要在消息、图片或日志中分享代理密码。应用与机器人的代理分别管理。另见[代理资料](../resources/proxies.md)。
